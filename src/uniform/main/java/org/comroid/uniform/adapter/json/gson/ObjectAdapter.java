package org.comroid.uniform.adapter.json.gson;

import com.google.gson.JsonObject;
import org.comroid.abstr.AbstractMap;

public class ObjectAdapter implements AbstractMap<String, Object> {
    public ObjectAdapter(JsonObject node) {
    }
}
