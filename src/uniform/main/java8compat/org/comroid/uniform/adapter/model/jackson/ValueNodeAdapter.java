package org.comroid.uniform.adapter.model.jackson;

import com.fasterxml.jackson.databind.node.ValueNode;
import org.comroid.uniform.model.ValueAdapter;

public class ValueNodeAdapter implements ValueAdapter {
    public ValueNodeAdapter(ValueNode node) {
    }
}
